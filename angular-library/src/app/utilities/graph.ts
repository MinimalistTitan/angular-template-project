// import { TinyColor } from '@ctrl/tinycolor';

// export const getSeriesColors = (count: number): Array<string> => {
//   const colors = [];
//   let color = new TinyColor('#D46A6A');
//   const step = 360 / count;

//   for (let i = 0; i < count; i++) {
//     colors.push(color.toHexString());
//     color = color.spin(step);
//   }
//   return colors;
// };
